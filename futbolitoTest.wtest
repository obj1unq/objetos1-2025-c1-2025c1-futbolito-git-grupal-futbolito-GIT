import objetos.*
import pgmProgram.*

describe "el fulbo"{
    test "hagamos un taquito"{
        pelota.position(lionel.position())
        lionel.taquito()
        assert.equals(pelota.position().x(), lionel.position().x()-2)
        assert.equals(pelota.position().y(), lionel.position().y())
    }
}